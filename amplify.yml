version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd price-view
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: price-view/.next
    files:
      - '**/*'
  cache:
    paths:
      - price-view/node_modules/**/*
